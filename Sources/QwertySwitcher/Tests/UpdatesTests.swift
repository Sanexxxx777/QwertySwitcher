import Foundation

/// Stub registered up front (10.09.2026) so TestRunner.swift is edited exactly
/// once by the orchestrator; the body is filled in by wave W1-B (opt-in updater + diagnostics export).
enum UpdatesTests {
    static func run() {}
}
