// XCTest is not available without Xcode (only Command Line Tools installed).
// Real unit tests will be added once Xcode is installed.
// For now we keep the file as a placeholder so swift build still succeeds.
import Foundation

@inline(__always)
func _swiftTestingPlaceholder() {
    _ = SashaSwitcherTestsVersion
}

let SashaSwitcherTestsVersion = "0.2.0"
